User → Frontend
     → reCAPTCHA
     → Backend
         → get IP
         → hash IP
         → check limits
         → sign message
     → Solidity
         → verify signature
         → enforce IP rules
